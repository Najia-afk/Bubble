# api/algorithms/ — Pure algorithms, no DB access
# Each module is a set of pure functions or stateless classes.
# Input = data (dicts, lists, DataFrames). Output = results.
# No SQLAlchemy sessions, no side effects.
